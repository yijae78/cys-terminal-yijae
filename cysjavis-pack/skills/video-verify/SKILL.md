---
name: video-verify
description: 완성 영상을 업로드 전 시각적으로 검증·승인하는 대표 스킬 — 프레임 추출 후 비전 검수로 시각 품질·모션그래픽 타이밍·오디오 싱크·대본 사실성 4관문을 돌려 전부 GO일 때만 출고한다. "영상 검증 / 시각 검수 / 영상 QA / 업로드 전 확인 / verify video" 트리거, 또는 youtube-video-pipeline 5단계로 발동.
---

# video-verify

"내가 검토조차 안 해도 된다"는 목표(800k 채널 평판)를 **단계별 기계 게이트**로 환원한다.
영상을 프레임으로 펼쳐 비전으로 검수하고, 4관문이 전부 GO여야 출고한다. 한 관문이라도
NO_GO면 원인 단계로 되돌린다(다듬기로 덮지 않는다).

> 정직한 한계: 자동 검수는 명백한 결함(이탈·끊김·싱크 어긋남·무근거 자막)을 잡지만,
> "완벽"의 마지막 1%는 초기 몇 회 사람 스팟체크로 보정된다 — 신뢰는 검증 누적으로 번다.

## 하위 스킬 오케스트레이션 (4관문 — 전부 GO 필요)

1. `[[video-verify-visual]]` — 프레임 추출 + 시각 검수(이탈·겹침·아바타 가시·미적).
2. `[[video-verify-timing]]` — 모션그래픽 in/out이 나레이션·타임라인과 정확히 맞는가.
3. `[[video-verify-audio-sync]]` — 립싱크·오디오 정렬·끊김.
4. `[[video-verify-final-gate]]` — 자막·화면 텍스트의 사실성 + 종합 go/no-go.

## 절차

1. **프레임 추출** → 검증: `final/video.mp4`를 일정 간격(예: 1~2fps) + 전환 지점에서 프레임
   추출. 오디오 트랙 분리.
2. **4관문 실행** → 검증: 위 4개 하위 스킬을 돌려 각 관문의 [GO/NO_GO + 근거 프레임·시각]을 수집.
3. **루프** → 검증: NO_GO 관문이 있으면 원인 단계(대본/음성/아바타/합성)로 되돌려 수정 →
   재합성 → 재검증. 전부 GO일 때까지 반복(상한·escalation 명시).
4. **승인** → 검증: 4관문 전부 GO면 `final/video.mp4`를 승인하고 `verify-report.md` 발행.

## 출력 계약

`verify-report.md` — 관문별 [판정·근거·문제 프레임] + 종합 판정. 4관문 전부 GO여야 상위
`[[youtube-video-pipeline]]`가 종료·출고 승인. 미통과 시 종료 금지·원인 단계로 회송.

**증거 규약 (영상/음성 매체 — NO_GO 근거 의무)**: 각 결함 근거는 필수 증거 3종을 갖춘다 —
① 타임코드 구간 · ② 프레임 캡처 경로 · ③ 결함 서술. verdict evidence 로 낼 때 위치 ref 규약은
`경로.mp4#t=MM:SS-MM:SS`(영상 경로 + 타임코드 구간 융합), 프레임 캡처는 별도 evidence 항목
(`ref`=`frames/f_MMSS.png`). 결함을 지적하는 verdict(NO_GO→REVISE/BLOCK)는 실행가능한 `fix`를
동반해야 한다(미동반 시 자동 강등). 상세·예시 JSON = `${CYS_PACK_DIR:-$HOME/.cys/pack}/round/EVIDENCE_CONVENTION.md`(§1 영상/음성).
